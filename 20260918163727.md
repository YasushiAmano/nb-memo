# 監視ツールのLaravel化

mover-schedule を参考にLaravel環境構築

https://github.com/moversystem/mover-schedule/tree/main

## 1. 構成

| コンテナ | 中身 | ホスト側ポート | EC2本番での対応 |
|---|---|---|---|
| nginx | nginx 1.27 | **8000** | nginx（80番） |
| app | Rocky Linux 10 + Apache + PHP-FPM 8.3 + Laravel 13 | (8080 内部) | Apache + PHP-FPM（8080番） |
| go-api | Go 1.26 + Gin（air でホットリロード） | (8081 内部) | Go + Gin（8081番・systemd） |
| postgres | PostgreSQL 16 | **15432** | PostgreSQL 16（127.0.0.1のみ） |
| redis | Redis 7 | **16379** | **Valkey 8**（後述） |
| mailpit | メール確認用 | **8025** | Amazon SES |


